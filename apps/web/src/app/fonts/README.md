# Local Fonts

Place `Inter-VariableFont_slnt,wght.woff2` here for the Inter font.

Download from: https://fonts.google.com/specimen/Inter
(Click "Download family", extract the .woff2 variable font file)

Until the font file is added, the app uses the system sans-serif fallback via CSS.
